<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vacabulary_book">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="org.litepal.LitePalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".localwords"
            android:label="@string/title_activity_localwords"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".word_detail"
            android:label="@string/title_activity_word_detail"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".wordContentActivity_danye" />
        <activity
            android:name=".EnglishNews"
            android:label="@string/title_activity_english_news"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>
</manifest>